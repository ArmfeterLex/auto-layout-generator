# auto-layout-generator
Приложение на C# Windows Forms для автоматического создания элементов управления в строку с переносом. Случайно выбирает тип (кнопка или многострочное поле). Кнопка увеличивает высоту всех текстовых полей в два раза.
